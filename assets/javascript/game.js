// Predefine variables as undefined to be redefined later 
var words, currentWord, userGuess, lettersGuessed, lettersMatched, remainingGuesses, wins, availableLetters, win, loss


function setup(){
words = ["words", "that", "are", "part", "of", "the", "theme"]
console.log(words)
}
setup()
console.log(words)



// var userGuess = event.key.toLowerCase();

// currentWord = words[Math.floor(Math.random() * words.length)];

